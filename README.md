# SQL_API

An ASP.NET Core Web API that retrieves student details from a SQL Server database.

---

## 📦 Prerequisites
- .NET 6 SDK
- SQL Server (local or remote instance)  
- A `STUDENT` table with columns: `REGNO`, `NAME`, `EMAIL`, `PHONE`, `DEPARTMENT`  
- Postman or curl for testing
